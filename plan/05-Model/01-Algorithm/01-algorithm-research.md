# Algorithm Research

## 1. Purpose

Identify and research machine learning algorithms suitable for the 2048 game. The goal is to find the best supervised learning algorithm that can approximate optimal play by treating board state → action selection as a classification problem.

## 2. Research Scope

Investigate supervised learning algorithms available in automl's `ModelType` enum. The automl framework provides tree-based, linear, and kernel-based models — all supervised learners. These are used to classify which move (up, down, left, right) is optimal given a board state.

```mermaid
flowchart TD
    subgraph "Algorithm Research"
        subgraph "Tree-Based Models"
            RF[Random Forest]
            GBM[Gradient Boosting]
            XGB[XGBoost]
            LGBM[LightGBM]
            ET[Extra Trees]
            DT[Decision Tree]
        end
        
        subgraph "Linear Models"
            LR[Logistic Regression]
            SVM[Support Vector Machine]
            SGD[Stochastic Gradient Descent]
            KNN[K-Nearest Neighbors]
        end
        
        subgraph "Ensemble Methods"
            VB[Voting Classifier]
            ST[Stacking]
            BG[Bagging]
        end
    end
    
    RF --> Evaluation
    GBM --> Evaluation
    XGB --> Evaluation
    LGBM --> Evaluation
    ET --> Evaluation
    DT --> Evaluation
    LR --> Evaluation
    SVM --> Evaluation
    SGD --> Evaluation
    KNN --> Evaluation
    VB --> Evaluation
    ST --> Evaluation
    BG --> Evaluation
    
    Evaluation[Evaluation & Benchmarking]
```

## 3. Algorithm Candidates

### 3.1 Supervised Learning for Sequential Games

2048 is a sequential decision-making game, but automl provides only supervised learning models. We bridge this gap by framing the problem as **multi-class classification**:

- **Input**: A 25-dimensional feature vector representing the board state
- **Output**: One of 4 actions (up, down, left, right)
- **Training data**: Generated by simulating games and recording (state, best_move) pairs

Each board state maps to a single optimal action. By collecting enough state-action pairs, we train a classifier that predicts the best move given any board configuration. This approach does not learn a value function or policy network as in RL — it directly learns the mapping from states to actions.

```mermaid
flowchart TD
    subgraph "Supervised Classification Pipeline"
        State[Board State<br/>25 Features]
        Label[Optimal Action<br/>4 Classes]
        
        State --> Model[Classifier]
        Label --> Model
        Model --> Prediction[Predicted Action]
    end
    
    style State fill:#e1f5fe
    style Label fill:#e1f5fe
    style Prediction fill:#fff9c4
```

### 3.2 Tree-Based Models

Tree-based models are the strongest candidates due to their ability to handle non-linear relationships in board state features and their inherent capacity to capture interaction effects between features (e.g., monotonicity × empty tiles).

```mermaid
flowchart LR
    A[Board State Features] --> B{Tree-Based Models}
    B --> C[Random Forest]
    B --> D[Gradient Boosting]
    B --> E[XGBoost]
    B --> F[LightGBM]
    B --> G[Extra Trees]
    B --> H[Decision Tree]
    C --> I[Evaluation]
    D --> I
    E --> I
    F --> I
    G --> I
    H --> I
```

### 3.3 Linear and Kernel Models

Linear models and SVMs provide baselines that are faster to train and may generalize better with limited data, though they cannot capture complex feature interactions as effectively as tree-based models.

```mermaid
flowchart LR
    A[Board State Features] --> B{Linear / Kernel Models}
    B --> C[Logistic Regression]
    B --> D[SVM]
    B --> E[SGD Classifier]
    B --> F[KNN]
    C --> G[Evaluation]
    D --> G
    E --> G
    F --> G
```

### 3.4 Ensemble Methods

Ensemble methods combine multiple base classifiers to improve prediction accuracy and robustness.

```mermaid
flowchart TD
    subgraph "Ensemble Methods"
        VB[Voting Classifier<br/>Hard/Soft Voting]
        ST[Stacking<br/>Meta-Learner]
        BG[Bagging<br/>Bootstrap Aggregation]
    end
    
    VB --> Evaluation
    ST --> Evaluation
    BG --> Evaluation
```

## 4. Evaluation Criteria

| Criterion | Weight | Description |
|-----------|--------|-------------|
| Mean Score | 35% | Average final game score across test games |
| Median Score | 20% | Median final game score (robust to outliers) |
| Classification Accuracy | 20% | Accuracy of move prediction on held-out states |
| Inference Speed | 15% | Time to predict a move (ms) |
| Generalization | 10% | Performance on unseen board configurations |

**Note on automl capabilities**: automl provides supervised classification and regression models. It does not include RL algorithms (Q-Learning, DQN, PPO). All model selection is done via automl's `ModelType` enum which includes DecisionTree, RandomForest, GradientBoosting, XGBoost, LightGBM, SVM, KNN, LogisticRegression, SGD, AdaBoost, ExtraTrees, and others.

## 5. Research References

All algorithm research findings will be documented in `05-Model/01-Algorithm/`:

```mermaid
flowchart TD
    Research[05-Model/01-Algorithm]
    Research --> N01[01-algorithm-research.md]
    Research --> N02[02-model-comparison.md]
    Research --> N03[03-best-algorithm-finding.md]
```

## 6. Next Steps

1. Train and benchmark candidate algorithms using automl's supervised learning models
2. Compare model performance metrics on classification accuracy and game score
3. Select the best performing algorithm
4. Document findings in `05-Model/01-Algorithm/03-best-algorithm-finding.md`
